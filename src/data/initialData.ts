import { Building, PropertyReport } from '../types';

// Clean initial data without mock examples
export const INITIAL_BUILDINGS: Building[] = [];
export const INITIAL_REPORTS: PropertyReport[] = [];
